export enum RoleTypes {
  ADMIN = 'Admin',
  USER = 'User',
  GUESS = 'Guess',
}

export enum PermissionTypes {
  FLATFORM_ADMIN = 'FLATFORM_ADMIN',
  COMMENT = 'COMMENT',
  POST = 'POST',
  PRODUCT = 'PRODUCT',
}

export enum SystemRoleLevel {
  SUPER_ADMIN = 1,
  ORGANIZATION_ADMIN = 2,
  STANDARD_USER = 3,
}

export enum RequiredInTypes {
  BODY = 'body',
  QUERY = 'query',
  PARAMS = 'params',
}

export enum Sex {
  HEIFER = 'Heifer',
  STEER = 'Steer',
  COW = 'Cow',
  BULL = 'Bull',
}

export enum LocationType {
  FEED_YARD = 'Feed Yard',
  DAIRY_FARM = 'Dairy Farm',
}

export enum LightColor {
  GREEN = 'Green',
  RED = 'Red',
}
